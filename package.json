{
  "name": "lagom-game-template",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "tsc && vite build && butler push dist quackqack/bin-chicken:html5"
  },
  "dependencies": {
    "lagom-engine": "^1.4.0-beta.4",
    "webfontloader": "^1.6.28"
  },
  "devDependencies": {
    "@types/webfontloader": "^1.6.34",
    "typescript": "^5.6.2",
    "vite": "^5.4.7"
  }
}
